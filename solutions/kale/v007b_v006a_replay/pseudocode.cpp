/*
KALE V007B — EXACT V006A REPRODUCIBILITY REPLAY

Copy the V006A algorithm and upload bytes exactly:
    V005A isolated T2 helper
    commit up to two independent endpoint welds from one final T2 frontier

No algorithm or source byte changes. Purpose: determine whether the prior
test-4 failure reproduces despite T3 machine code being identical to V005A.
*/
