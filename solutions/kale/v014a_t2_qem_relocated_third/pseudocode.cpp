/*
KALE V014A — T2 QEM-RELOCATED THIRD COLLAPSE

Exact base: V010A. Preserve every non-T2 branch, especially the proven T3
five-strike schedule, without adding another T3 operation.

Inside only the isolated T2 terminal helper:
    run the existing original-reference search
    render original versus current and build perceptual debt
    commit V010A's existing independent pair unchanged

Then rebuild the current frontier and debt once. Inspect the first eight legal
directed endpoint candidates for one additional sequential collapse. For every
candidate:
    sum incident plane quadrics
    compute the segment-constrained QEM optimum
    compare it with midpoint, excluding both old endpoints
    retain the lower-loss legal interior position under full-context local
        1024 normal/depth SSIM
    preserve face area/orientation, duplicate, manifold, inherited-radius,
        coverage-restoration, and positive-gain checks

Commit exactly one candidate: the candidate/interior placement with minimum
incremental rendered loss. Do not reward perceptual debt for this extra probe.

This revisits the previously unsafe T2 triplet as a sequential, relocated third
collapse while leaving the established independent pair and all T3 behavior
unchanged.
*/
