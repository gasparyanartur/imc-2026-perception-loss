/*
KALE V006A — T2 INDEPENDENT-PAIR TERMINAL FLANK

Exact control: Kale V005A, 90.545792, PPPPPPP.

ALGORITHM
    execute the complete V005A pipeline unchanged
    inside only the section-isolated T2 terminal helper:
        build original-relative debt and one legacy endpoint-weld frontier
        inspect the first eight candidates and move the minimum
            crop-area disruption minus debt candidate to the front
        scan the legacy-sorted frontier once
        commit up to two mutually independent directed endpoint welds
            require the unchanged score cap 0.30
            lock both endpoints and both one-rings after each selection
            retain all duplicate-face, manifold, area, Hausdorff-coverage,
                and positive-gain reconstruction guards

INTERPRETATION
    Two spatially independent late edits can share the expensive debt render
    and full reconstruction. This tests parallel composition, not a deeper
    sequential frontier. The edit is an equal-byte `1 -> 2` substitution in
    the cold T2 helper; all executed T3 code and translation-unit size remain
    unchanged.
*/
