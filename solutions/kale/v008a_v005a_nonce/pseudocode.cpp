/*
KALE V008A — EXECUTABLE-EQUIVALENT V005A TIMING REPLAY

Use the exact V005A algorithm and source, adding only a unique comment so the
submission service creates a new job. Before upload, compile and strip debug
metadata; require executable bytes and section sizes to match V005A, with only
build-identity metadata allowed to differ. No algorithm or runtime change.
*/
