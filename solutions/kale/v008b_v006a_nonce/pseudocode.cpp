/*
KALE V008B — EXECUTABLE-EQUIVALENT V006A TIMING REPLAY

Use the exact V006A independent-pair algorithm and source, adding only a unique
comment so the submission service creates a new job. Verify executable-byte
equivalence to V006A before upload. No algorithm or runtime change.
*/
